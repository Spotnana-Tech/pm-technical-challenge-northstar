/**
 * The "no scenario" identity, shared so the scenario picker and the engine's
 * fallback context never disagree on the id or the label — one string, one
 * place.
 */
export const NONE_SCENARIO = { id: 'none', label: 'Baseline (no scenario)', knobs: {} }
